[workspace]
members = [
    "rust_lib/chemistry_core",
    "rust_lib/vendor/mid-math",
    "rust_lib/vendor/mid-collections",
]
resolver = "2"

[profile.release]
opt-level     = 3
lto           = true
codegen-units = 1
panic         = "abort"
strip         = true
debug         = false
