Chemistry simulation bindings live here: atom state, atom registry, and the
FFI calls into `chemistry_core` (spatial hash / LJ forces / velocity Verlet).
Added once `rust_lib/chemistry_core`'s Rust API is finalized.
