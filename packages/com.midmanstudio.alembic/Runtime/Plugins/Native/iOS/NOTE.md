Expected build artifact for this platform: `libchemistry_core.a (device) + libchemistry_core_sim.a (simulator)`

Built from `rust_lib/chemistry_core` — see the platform-build CI once it
exists (mirrors MidManStudio_Unity's build-and-commit pattern for
`projectile_core`). Nothing here yet; the Rust simulation core isn't
built out.
